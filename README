1. Add a .env at the root of the infra project with DATABASE_URL=postgres://username:password@localhost:5432/name-postgres changing for username, password and the name of your posgres database.
2. cargo build
3. cargo run --bin write_objects_from_xlsx
